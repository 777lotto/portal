{
  "name": "notification",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --port 8789 --inspector-port 9230",
    "start": "wrangler dev",
    "build": "echo 'Build check passed'",
    "typecheck": "tsc --noEmit --skipLibCheck"
  },
  "dependencies": {
    "@portal/shared": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250525.0",
    "rimraf": "^5.0.10",
    "typescript": "^5.5.2",
    "wrangler": "^4.13.2"
  }
}
