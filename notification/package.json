{
  "name": "notification",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "prebuild": "cd ../packages/shared && pnpm run build",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "build": "pnpm run prebuild && tsc --noEmit"
  },
  "dependencies": {
    "@portal/shared": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250424.0",
    "rimraf": "^5.0.10",
    "typescript": "^5.5.2",
    "wrangler": "^4.13.2"
  }
}
