{
  "name": "notification",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy --env production",
    "deploy:dev": "wrangler deploy",
    "dev": "wrangler dev --port 8789 --inspector-port 9230 --env development",
    "start": "wrangler dev",
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@portal/shared": "workspace:*",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250724.0",
    "@types/web-push": "^3.6.3",
    "rimraf": "^5.0.10",
    "typescript": "^5.5.2",
    "wrangler": "^4.13.2"
  }
}
