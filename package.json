{
  "name": "portal",
  "version": "0.1.0",
{
  "name": "portal",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "frontend",
    "worker",
    "packages/shared",
    "notification",
    "payment"
  ],
  "scripts": {
    "shared:build": "cd packages/shared && pnpm run build",
    "shared:dev": "cd packages/shared && pnpm run dev",

    "worker:dev": "cd worker && pnpm run dev",
    "worker:build": "cd worker && pnpm run build",
    "worker:deploy": "cd worker && pnpm run deploy",
    "worker:typecheck": "cd worker && tsc --noEmit --skipLibCheck",

    "notification:dev": "cd notification && pnpm run dev",
    "notification:build": "cd notification && pnpm run build",
    "notification:deploy": "cd notification && pnpm run deploy",
    "notification:typecheck": "cd notification && tsc --noEmit --skipLibCheck",

    "payment:dev": "cd payment && pnpm run dev",
    "payment:build": "cd payment && pnpm run build",
    "payment:deploy": "cd payment && pnpm run deploy",
    "payment:typecheck": "cd payment && tsc --noEmit --skipLibCheck",

    "frontend:dev": "cd frontend && pnpm run dev",
    "frontend:build": "cd frontend && pnpm run build",
    "frontend:deploy": "cd frontend && pnpm run build && pnpm exec wrangler pages deploy dist --project-name=portal",
    "frontend:typecheck": "cd frontend && tsc --noEmit --skipLibCheck",

    "dev": "concurrently \"pnpm run shared:dev\" \"pnpm run worker:dev\" \"pnpm run frontend:dev\" \"pnpm run notification:dev\" \"pnpm run payment:dev\"",
    
    "typecheck": "pnpm run shared:build && concurrently \"pnpm run worker:typecheck\" \"pnpm run notification:typecheck\" \"pnpm run payment:typecheck\" \"pnpm run frontend:typecheck\"",

    "build": "pnpm run shared:build && concurrently \"pnpm run worker:build\" \"pnpm run notification:build\" \"pnpm run payment:build\" && pnpm run frontend:build",

    "deploy:workers": "concurrently \"pnpm run worker:deploy\" \"pnpm run notification:deploy\" \"pnpm run payment:deploy\"",
    
    "migrate": "cd worker && pnpm exec wrangler d1 migrations apply gutter_db --env production",

    "deploy": "pnpm install && pnpm run build && pnpm run migrate && pnpm run deploy:workers && pnpm run frontend:deploy" 
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
