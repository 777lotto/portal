// worker/src/env.ts - Remove this file since we're using types from @portal/shared
// This file is no longer needed - all environment types are in @portal/shared/src/types.ts

// If you need worker-specific environment extensions, you can create them here:
// import type { Env as BaseEnv } from "@portal/shared";
// 
// export interface WorkerEnv extends BaseEnv {
//   // Worker-specific environment variables
// }

export {}; // Make this an empty module
