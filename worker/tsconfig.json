{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    /* target & libs for Workers */
    "target": "ES2022",
    "lib": ["ES2023"],

    /* noEmit (we bundle via Wrangler) */
    "noEmit": true
  },
  "include": [
    "src",
    "../worker-configuration.d.ts"
  ]
}
