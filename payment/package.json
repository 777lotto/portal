{
  "name": "payment",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --port 8790 --inspector-port 9231",
    "start": "wrangler dev",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@portal/shared": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250424.0",
    "rimraf": "^5.0.10",
    "typescript": "^5.5.2",
    "wrangler": "^4.13.2"
  }
}
