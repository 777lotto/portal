interface Props {
    token: string | null;
    setToken: (token: string | null) => void;
}
export default function Navbar({ token, setToken }: Props): import("react/jsx-runtime").JSX.Element;
export {};
